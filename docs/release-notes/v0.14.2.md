# v0.14.2

Improves the update download experience with a visible progress bar.

- **Download progress bar** — the About panel now shows a progress bar while downloading an update. When the server reports a file size you see a determinate bar with percentage (e.g. `45%`) and the bytes count (`12.3 MB / 28.0 MB`); when the size is unknown an animated sweep indicates activity.
