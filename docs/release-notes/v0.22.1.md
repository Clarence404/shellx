# v0.22.1 — Tests on every push

Nothing in the app changed. This release exists so the version number matches the tree that now has continuous integration behind it.

- **The tests run on GitHub.** Every push and pull request to `main` typechecks the frontend, runs the frontend test suite, and runs the Rust test suite on Linux. Until now the only workflow built installers without running a single test.
