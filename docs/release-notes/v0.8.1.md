v0.8.1 polishes the sidebar chrome introduced in v0.8.0.

- **Section headers** — Hosts and Settings sidebars now share a consistent header style: separator line, brighter uppercase label, extracted into a reusable `SectionHeader` component.
- **Hosts rail icon** — replaced the generic monitor icon with a server icon that better represents the SSH host list.
